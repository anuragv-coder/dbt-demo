version: 2
models:
  - name: t1
    description: This table contains clickstream events from the marketing website
    columns:
        - name: country
          description: Contains the name of country
          tests:
              - unique
              - not_null
        - name: zip
          quote: true
          description: Contains the zip code
          tests:
              - not_null